mutation ChangePassword(
	$token: String!
	$newPassword: String!
	$newPassword1: String!
) {
	changePassword(
		token: $token
		newPassword: $newPassword
		newPassword1: $newPassword1
	) {
		...RegularUserResponse
	}
}
